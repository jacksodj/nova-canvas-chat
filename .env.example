# PostgreSQL password for LiteLLM database
POSTGRE_PASSWORD=your_secure_password_here

# AWS Bedrock API Key (Bearer Token)
# Generate your API key at: https://console.aws.amazon.com/bedrock/api-keys
# Choose "Long-term" for persistent development or "Short-term" for testing (12 hours)
AWS_BEARER_TOKEN_BEDROCK=your_bedrock_api_key_here

# Alternative: You can also use AWS IAM credentials instead of API keys
# If using IAM credentials, comment out AWS_BEARER_TOKEN_BEDROCK and ensure
# your ~/.aws/credentials file is configured:
#   aws configure
